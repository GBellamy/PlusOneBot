app = "discord-plusone"
primary_region = "cdg"

[http_service]
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1

[build]

[[vm]]
  size = "shared-cpu-1x"
  memory = "256mb"
